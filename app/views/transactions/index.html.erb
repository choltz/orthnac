<div class="transaction-page">
  <row>
    <div class="col s12">
      <div class="card">
        <div class="card-content">
          <span class="card-title">Transaction History</span>

          <table class="transaction-table bordered striped">
            <thead>
              <tr>
                <th class="center-align">Import Id</th>
                <th>Transaction date</th>
                <th>Merchant</th>
                <th>Category</th>
                <th class="right-align">Amount</th>
              </tr>
            </thead>
            <tbody>
              <% @transactions.each do |transaction| %>
                <tr>
                  <td class="center-align"><%= transaction.import_id %></td>
                  <td><%= transaction.transaction_at.strftime('%a, %m/%d/%Y') %></td>
                  <td><%= transaction.merchant_name %></td>
                  <td><%= transaction.category %></td>
                  <td class="right-align"><%= format_amount(transaction.amount) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>

        </div>
      </div>
    </div>
  </row>
</div>
